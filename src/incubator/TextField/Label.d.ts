import { LabelProps } from './types';
declare const Label: {
    ({ label, labelColor, labelStyle, labelProps, validationMessagePosition, floatingPlaceholder, testID }: LabelProps): any;
    displayName: string;
};
export default Label;
