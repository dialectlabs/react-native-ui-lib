import { ValidationMessageProps } from './types';
declare const ValidationMessage: {
    ({ validationMessage, enableErrors, validationMessageStyle, retainValidationSpace, validate, testID }: ValidationMessageProps): JSX.Element | null;
    displayName: string;
};
export default ValidationMessage;
